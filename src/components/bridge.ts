// import { Recipe } from "~/components";

// export class Bridge {
//   protected port?: chrome.runtime.Port;
//   private registeredListeners = new Set<(recipe: Recipe) => void>();
//   constructor() {}
//   protected setPort(port: chrome.runtime.Port) {
//     this.port = port;
//   }
//   protected registerListener(cb: (recipe: Recipe) => void) {
//     this.registeredListeners.add(cb);
//   }
// }
